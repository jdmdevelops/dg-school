### Images
##### Title header backgrounds
- In an effort to not have to continue re-learning this every time I have to create one, the process is: receive image via AirDrop (portait aspect ratio) => open with Preview => crop using rectangular selection
